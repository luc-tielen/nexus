# Instructions

- Always write code (variable names, function names, comments, strings, file names, etc.) in English, regardless of the language used in user instructions.
- For scheduling and cron tasks, always use `mcp__nexus__add_cron` / `mcp__nexus__delete_cron` / `mcp__nexus__list_crons`. Never use the `schedule` skill or any other scheduling system.
